'use strict';

/**
 *  contrats-bail-mobilite controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::contrats-bail-mobilite.contrats-bail-mobilite');
