'use strict';

/**
 * contrats-bail-mobilite service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::contrats-bail-mobilite.contrats-bail-mobilite');
