'use strict';

/**
 *  contrats-residence-secondaire controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::contrats-residence-secondaire.contrats-residence-secondaire');
