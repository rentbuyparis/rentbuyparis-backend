'use strict';

/**
 * contrats-residence-secondaire service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::contrats-residence-secondaire.contrats-residence-secondaire');
